let snowman : AnimatedItem = Scene.getItem("rRnCVJWP") as AnimatedItem;

let text : string = "Preciso que monte um computador. Use 'F' para pegar os componentes e colocá-los na mesa e 'E' para abrir portas".toString();

snowman.speech = text.toString();
